{
  "name": "@personal-os/api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "echo 'Edge Functions deploy directly to Supabase — use: npm run deploy'",
    "deploy": "supabase functions deploy",
    "type-check": "echo 'Edge Functions use Deno - type checking skipped in Node environment'"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "typescript": "^5.3.3"
  }
}
